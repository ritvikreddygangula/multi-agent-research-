---
paths:
  - "frontend/**/*.{js,jsx,ts,tsx,css}"
---

# Frontend rules

- Keep UI minimal and readable.
- Prefer composition over large monolithic components.
- Loading, error, and success states must all be handled.
- Any new backend fields must be rendered safely.
- Do not assume every research branch succeeded.
- Keep naming clear and prop contracts small.